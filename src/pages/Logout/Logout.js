import { Box, Typography } from "@mui/material";

export default function Logout() {
  return (
    <Box sx={{ textAlign: "center", paddingTop: "15em" }}>
      <Typography variant="h1">Goodbye, you logged out.</Typography>
    </Box>
  );
}
